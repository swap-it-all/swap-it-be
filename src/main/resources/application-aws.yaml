cloud:
  aws:
    s3:
      bucket: ${S3_BUCKET_NAME}
    stack:
      auto: false

    redis:
      host: ${REDIS_HOST}
      port: 6379

spring:
  servlet:
    multipart:
      max-file-size: 5MB # default 1MB
      max-request-size: 10MB